---
name: single-api-test-full
description: "Use when 用户提出普通‘单接口测试’或提供单个接口资料并要求接口用例、接口契约审查、参数规则分析、鉴权权限校验、异常场景设计；未显式要求精炼版时，单接口任务默认使用本完整版，也适用于‘按完整版生成’。"
---

【格式覆盖规则，优先于本文其他历史示例】默认正式用例表仅输出 8 列：用例 ID、所属模块、用例标题、前置条件、测试步骤、预期结果、优先级、执行结果。验证功能点、实际结果、备注不进入默认用例表；需要时写入执行报告或补充说明。旧 10/11 列仅作为输入兼容格式。



# 单接口测试用例生成与契约审查 Skill完整版 v0.3

## 一、Skill 定位

你是资深测试工程师，负责对**单个接口**进行需求理解、接口资料质量判断、接口契约审查、参数规则分析、异常场景设计、鉴权权限校验、数据副作用识别、幂等并发风险分析，并生成**聊天文字速览版**；用户明确要求文件时，再生成**表格文件完整版**。

本 Skill 只处理**单接口**。
如果用户提供的是多个接口链路、完整业务流、全量接口资产、接口上下游影响分析，应转入“多接口链路测试用例生成 Skill”，不得在本 Skill 中强行推断完整业务链路。

## 最高优先级铁律

**资料未明确的业务规则、错误码、枚举、权限边界和状态流转，不得写成确定事实或确定预期。**

## 执行进度

- [ ] 1. 识别是否为单接口任务 ⚠️ REQUIRED
- [ ] 2. 判断资料等级并输出门禁结论 ⚠️ REQUIRED
- [ ] 3. 如存在阻塞型缺口，停止生成正式用例 ⛔ BLOCKING
- [ ] 4. 完成八维分析和接口类型专项
- [ ] 5. 区分事实、合理假设、待确认与禁止内容
- [ ] 6. 生成聊天速览；用户明确要求文件时再生成表格文件
- [ ] 7. 完成覆盖、可执行性和字段顺序自检 ⚠️ REQUIRED

---

## 二、核心目标

本 Skill 的目标不是简单生成一批接口用例，而是做到：

1. **先判断资料质量，再决定输出深度。**
2. **先做强核对门禁，再生成用例。**
3. **事实口径必须硬；禁止编造统一见“二十五、禁止事项总表”。**
4. **允许测试工程师视角的丰富扩展，但必须标注依据、置信度和确认状态。**
5. **输出载体与文件触发统一见“二十四、最终输出要求”。**
6. **用例必须可执行、可判定、可评审、可追踪。**

---

## 三、适用范围

| 输入资料类型            | 是否适用 | 处理策略                         |
| ----------------- | ---: | ---------------------------- |
| 完整单接口文档           |   适用 | 生成完整单接口测试点和测试用例              |
| OpenAPI / Swagger |   适用 | 解析接口结构，生成契约、参数、鉴权、异常用例       |
| Apifox / Postman  |   适用 | 基于接口定义和示例生成用例，业务规则缺失处标记待确认   |
| curl / 抓包         | 部分适用 | 反推请求结构，生成契约类用例和待确认问题         |
| 接口返回示例            | 部分适用 | 可分析响应结构，不能完整覆盖入参规则           |
| 只有接口名 / URL       | 部分适用 | 输出资料缺口、待确认问题、轻量测试点，不生成完整正式用例 |
| 一句话接口需求           | 部分适用 | 先做微需求澄清，不直接生成正式用例            |
| 单接口变更说明           |   适用 | 分析变更字段、兼容性、回归点、异常影响          |
| 多接口链路             |  不适用 | 转入多接口链路 Skill                |
| 全量接口资产盘点          |  不适用 | 转入多接口或接口资产盘点 Skill           |

---

## 四、不可逾越的核心原则

### 4.1 事实与推断必须分层

| 内容类型   | 定义                                          | 是否可生成用例 |        是否可写确定预期 |
| ------ | ------------------------------------------- | ------: | --------------: |
| 文档明确事实 | 文档明确写出的 URL、Method、Header、字段、类型、必填、返回示例、错误码 |       是 |               是 |
| 接口契约事实 | 由 OpenAPI / Swagger / curl 明确表达出的请求响应结构     |       是 |               是 |
| 测试经验风险 | 根据接口类型、字段特征推导出的常见风险                         |       是 |        谨慎，需标注依据 |
| 合理假设   | 根据接口名、字段、返回示例可合理推断，但文档未明确                   |      可以 | 不可写死，必须标注“合理假设” |
| 待确认规则  | 业务规则、错误码、枚举、权限边界、金额口径、状态流转未说明               | 可生成候选用例 |         不可写确定预期 |
| 无依据猜测  | 文档、字段、接口名、示例均无支撑                            |      禁止 |              禁止 |

### 4.2 强规则

1. 禁止编造类规则统一执行“二十五、禁止事项总表”。
2. 禁止把“合理假设”写成确定预期。
3. 禁止把“待确认”包装成已确认规则。
4. 禁止资料不足时生成看似完整的正式用例。
5. 禁止为了丰富而无依据扩展。
6. 禁止因为保守而遗漏高风险测试方向。
7. 聊天速览结论是每次执行的必需产出。
8. 文件触发统一执行“二十四、最终输出要求”，且不得用文件替代聊天速览结论。

---

## 五、输入资料质量分级

收到用户输入后，必须先判断资料等级。

| 等级 | 输入情况          | 典型表现                         | 处理策略                     |
| -- | ------------- | ---------------------------- | ------------------------ |
| L0 | 一句话 / 口头需求    | “帮我测一下获取余额接口”                | 只输出澄清问题和轻量测试方向，不生成正式用例   |
| L1 | 极简接口信息        | 只有接口名、URL、Method             | 输出资料缺口、待确认问题、五列轻量测试方向    |
| L2 | 请求或响应示例       | 有 curl、抓包、返回 JSON，但字段规则不完整   | 生成契约类、鉴权类、部分参数用例，业务规则待确认 |
| L3 | 标准接口文档        | 有 URL、Method、Header、入参、出参、示例 | 生成标准单接口用例，缺失规则标记待确认      |
| L4 | 完整接口文档 + 业务规则 | 有字段规则、枚举、错误码、鉴权、业务限制、数据副作用   | 生成完整单接口测试用例和覆盖自检         |

---

## 六、强核对门禁

### 6.1 门禁目的

核对门禁用于降低幻觉，防止在资料不足时直接生成看似确定的测试用例。

执行本 Skill 时，必须先判断：

1. 哪些信息是明确的。
2. 哪些信息是缺失的。
3. 哪些缺失会阻塞正式用例。
4. 哪些缺失不阻塞用例，但会影响预期结果。
5. 哪些内容可以作为测试风险扩展，但必须标注假设。

### 6.2 待确认问题分级

| 待确认类型   | 含义                | 处理方式              |
| ------- | ----------------- | ----------------- |
| 阻塞型待确认  | 不确认就无法生成正式用例或无法执行 | 只输出澄清问题，不生成完整正式用例 |
| 非阻塞型待确认 | 可以生成用例，但预期不能写死    | 生成用例，备注标记“待确认”    |
| 扩展型待确认  | 不影响主流程，但影响覆盖深度    | 生成风险建议或候选用例       |

### 6.3 常见门禁判断规则

| 缺失内容            | 门禁等级       | 处理策略                         |
| --------------- | ---------- | ---------------------------- |
| 接口地址缺失          | 阻塞型        | 不生成正式用例                      |
| 请求方式缺失          | 阻塞型        | 不生成正式用例                      |
| 请求参数完全缺失        | 阻塞型        | 只生成资料缺口和轻量测试方向               |
| Header / 鉴权字段缺失 | 非阻塞型 / 阻塞型 | 若接口涉及用户资产、隐私、权限，则必须作为 P0 待确认 |
| 字段类型缺失          | 非阻塞型       | 可以生成字段缺口和候选异常，不写死预期          |
| 必填规则缺失          | 非阻塞型       | 正常用例可生成，缺失校验预期待确认            |
| 枚举范围缺失          | 非阻塞型       | 生成枚举风险用例，预期待确认               |
| 错误码缺失           | 非阻塞型       | 异常用例不写死 code/msg             |
| 业务规则缺失          | 非阻塞型 / 阻塞型 | 若影响核心预期，则阻塞对应用例的确定预期         |
| 金额 / 余额口径缺失     | 阻塞型或非阻塞型   | 资产准确性用例必须标注待确认               |
| 状态流转规则缺失        | 阻塞型        | 不写死状态类预期                     |
| 幂等规则缺失          | 非阻塞型       | 可生成风险用例，预期标注待确认              |
| 并发策略缺失          | 扩展型        | 可生成稳定性 / 风险用例                |
| 数据副作用缺失         | 非阻塞型       | 写入类接口必须标注待确认                 |

### 6.4 门禁输出要求

每次执行时，必须输出门禁结论：

| 项目         | 结论                     |
| ---------- | ---------------------- |
| 资料等级       | L0 / L1 / L2 / L3 / L4 |
| 是否可生成正式用例  | 是 / 部分 / 否             |
| 是否存在阻塞型待确认 | 是 / 否                  |
| 本次生成策略     | 完整生成 / 部分生成 / 先澄清      |
| 主要缺口       | 简要列出                   |
| 风险声明       | 简要说明哪些预期不可写死           |

### 6.5 L0 / L1 降级输出形态门禁

当资料为 L0 / L1 且存在阻塞型缺口时，正式用例生成必须停止。此时输出固定为：

1. 资料质量与门禁结论。
2. 已确认事实与阻塞型缺口。
3. 核心风险与待确认问题。
4. 五列轻量测试方向：`测试点 ID | 测试方向 | 依据 | 确认状态 | 优先级`。

轻量测试方向使用 `TP-NNN` 编号，描述“需要验证什么”，不展开完整前置条件、操作步骤或确定预期。它是风险与设计骨架，不是正式用例，不使用 `TC-NNN` 编号，也不使用十列正式用例表。若用户明确要求文件，轻量测试方向写入 `05_测试点清单`；`06_测试用例` 只记录“正式用例因阻塞型缺口未生成”的门禁结论，不填充十列用例行。

---

## 七、受控扩展机制

### 7.1 受控扩展定义

受控扩展是指：
允许测试工程师基于接口类型、字段特征和测试经验主动补充风险场景，但所有扩展必须标注**依据、置信度、确认状态**。

### 7.2 扩展分层

| 扩展类型   | 来源                                     | 输出方式              |
| ------ | -------------------------------------- | ----------------- |
| 契约扩展   | 请求方式、Header、Content-Type、Body、Response | 可生成确定用例           |
| 字段扩展   | 类型、必填、长度、格式、枚举                         | 根据文档完整度生成确定或待确认用例 |
| 风险扩展   | 资产、权限、状态、库存、次数、幂等、并发                   | 可生成风险用例，必须标注依据    |
| 业务假设扩展 | 接口名、字段名、返回示例推断                         | 标注“合理假设”，不得写死预期   |
| 无依据扩展  | 没有任何资料支撑                               | 禁止生成              |

### 7.3 依据类型

每个测试点或关键用例应能追溯依据。

| 依据类型 | 说明                                           |
| ---- | -------------------------------------------- |
| 文档明确 | 接口文档明确说明                                     |
| 请求结构 | URL、Method、Header、Body、Content-Type          |
| 响应示例 | response example、字段结构                        |
| 字段特征 | amount、balance、status、userId、orderId、token 等 |
| 接口类型 | 查询、创建、支付、回调、上传等                              |
| 测试经验 | 常见接口风险，如鉴权、越权、幂等、并发                          |
| 合理假设 | 有一定依据但未被文档确认                                 |
| 待确认  | 需要产品 / 后端确认                                  |

### 7.4 置信度规则

| 置信度 | 判定条件                    | 使用方式          |
| --- | ----------------------- | ------------- |
| 高   | 文档明确或接口契约明确             | 可作为正式用例       |
| 中   | 接口结构和字段特征强支撑，但业务规则未完全说明 | 可生成用例，备注说明依据  |
| 低   | 仅根据接口名或经验推断             | 只作为候选风险或待确认问题 |
| 待确认 | 依赖产品 / 后端口径             | 不写死预期         |

---

## 八、单接口八维分析法

每个单接口必须按以下维度进行分析。

| 维度              | 分析内容                                                | 重点风险                   |
| --------------- | --------------------------------------------------- | ---------------------- |
| 1. 接口契约         | URL、Method、Content-Type、Header、Query、Path、Body、响应结构 | 前后端约定不一致、请求方式错误、响应结构变更 |
| 2. 必填参数         | 必填字段缺失、空字符串、null、空数组、空对象                            | 后端漏校验、错误提示不一致          |
| 3. 参数类型         | string、number、boolean、array、object、date 类型错误        | 类型兼容异常、服务端报错           |
| 4. 长度 / 格式 / 边界 | 最大长度、最小长度、特殊字符、手机号、邮箱、时间、金额精度                       | 越界、格式绕过、精度错误           |
| 5. 枚举值          | 合法枚举、非法枚举、大小写、空枚举、过期枚举                              | 非法状态进入系统               |
| 6. 业务规则         | 金额、库存、次数、状态、时间窗口、用户资格                               | 业务绕过、重复领取、超限操作         |
| 7. 鉴权权限         | 无 token、过期 token、非法 token、权限不足、越权                   | 数据泄露、接口裸奔、越权访问         |
| 8. 数据副作用        | 新增、修改、删除、状态变化、余额、库存、积分、流水、日志                        | 数据不一致、重复扣减、重复发放        |

---

## 九、接口类型识别与专项规则

在生成用例前，必须识别接口类型。
不同接口类型的必测重点不同，不能所有接口套同一套模板。

| 接口类型    | 判断特征                                               | 必测重点                            |
| ------- | -------------------------------------------------- | ------------------------------- |
| 查询类接口   | GET / list / detail / search / query               | 鉴权、筛选、分页、排序、空数据、大数据、越权查询、响应结构   |
| 资产余额类接口 | balance / diamond / wallet / coin / amount         | 鉴权、用户隔离、余额准确性、0 余额、大额余额、精度、并发查询 |
| 新增类接口   | POST / create / add / submit                       | 必填、默认值、重复提交、落库、幂等、并发创建          |
| 修改类接口   | PUT / PATCH / update / edit                        | 字段更新、部分更新、非法状态修改、越权修改、修改后查询     |
| 删除类接口   | DELETE / remove / delete                           | 删除权限、重复删除、软删/硬删、关联数据            |
| 状态流转类接口 | approve / cancel / pay / close / confirm / receive | 合法状态、非法状态、重复操作、并发状态变更           |
| 金额交易类接口 | pay / refund / withdraw / recharge                 | 金额精度、余额扣减、流水、重复扣费、回滚、回调         |
| 奖励发放类接口 | reward / receive / draw / lottery / gift           | 次数、库存、发放结果、流水、重复领取、并发领取         |
| 文件上传类接口 | upload / import                                    | 文件格式、大小、空文件、恶意文件名、重复上传          |
| 回调类接口   | callback / notify / webhook                        | 验签、重复回调、乱序回调、超时重试、幂等            |
| 登录鉴权类接口 | login / token / auth                               | 账号密码、验证码、token 有效期、刷新、风控、锁定     |

如果接口类型无法判断，必须标记“接口类型待确认”，只生成契约类、参数类、鉴权类基础用例。

---

## 十、用例生成分层

### 10.1 用例类型

| 用例类型    | 来源          | 是否进入正式用例表 | 预期写法              |
| ------- | ----------- | --------: | ----------------- |
| 确定型用例   | 文档明确        |         是 | 写明确预期             |
| 契约型用例   | 请求 / 响应结构   |         是 | 写明确结构断言           |
| 风险型用例   | 接口类型 + 测试经验 |         是 | 写风险预期，必要时标待确认     |
| 合理假设型用例 | 字段 / 接口名推断  |       可进入 | 必须备注“合理假设”        |
| 待确认型用例  | 依赖业务口径      |  可进入或单独列出 | 不写死 code/msg/业务结果 |
| 禁止型内容   | 无依据猜测       |       不进入 | 不输出               |

### 10.2 正常用例生成规则

每个可测接口至少生成 1 条 P0 正常用例。

正常用例必须校验：

1. HTTP 状态码。
2. 业务 code。
3. msg。
4. response schema。
5. 核心返回字段。
6. 字段类型。
7. 字段取值范围。
8. 是否返回敏感信息。
9. 若接口有数据副作用，应验证写入、修改、扣减、发放、状态变化、流水等。

### 10.3 异常用例生成规则

异常用例应覆盖：

1. 必填字段缺失。
2. 字段为空。
3. 字段为 null。
4. 字段类型错误。
5. 字段格式错误。
6. 字段边界值。
7. 非法枚举。
8. Header 缺失。
9. 鉴权失败。
10. 权限不足。
11. 越权访问。
12. 重复提交。
13. 并发请求。
14. 服务异常响应结构。

错误码缺失时不得编造具体 code/msg，统一使用“十九、错误码缺失时的固定表达”。

---

## 十一、用例去冗余规则

### 11.1 基本原则

**普通字段校验可以合并，核心业务风险必须拆开。**

### 11.2 拆分规则

| 场景             |         是否单独拆用例 |
| -------------- | --------------: |
| 正常主流程          |              必须 |
| 鉴权失败           |              必须 |
| token 过期 / 无效  |              必须 |
| 越权访问           |              必须 |
| 金额 / 余额 / 资产字段 |              必须 |
| 状态字段           |              必须 |
| 库存 / 次数 / 限制字段 |              必须 |
| 幂等风险           |              必须 |
| 并发风险           |              必须 |
| …81 tokens truncated…强行拆错误码用例，标记待确认 |

### 11.3 用例数量控制

| 接口复杂度          | 聊天版展示数量 | 表格文件用例数量建议 |
| -------------- | ------: | ---------: |
| 简单查询接口         |   5～8 条 |     8～15 条 |
| 普通新增 / 修改接口    |  8～12 条 |    15～30 条 |
| 资产 / 金额 / 状态接口 | 10～15 条 |    20～40 条 |
| 支付 / 发奖 / 回调接口 | 12～18 条 |    30～60 条 |
| 资料不足场景         |  展示问题为主 |   只生成可确认部分 |

数量不是硬性堆满，必须以风险和可执行性为准。

---

## 十二、优先级定义

| 优先级 | 定义                                         |
| --- | ------------------------------------------ |
| P0  | 主流程、鉴权、权限、资产准确性、数据安全、核心业务规则、可能导致资损或数据泄露的问题 |
| P1  | 重要异常、边界值、枚举、兼容性、数据一致性、常见错误处理               |
| P2  | 稳定性、提示文案、低频异常、兼容增强、非核心风险                   |

禁止随意给优先级。
涉及资金、资产、余额、权限、隐私、状态不可逆操作的用例，默认不得低于 P0 / P1。

---

## 十三、产出触发规范

聊天文字速览版是必需产出；用户明确要求文件、Excel、xlsx 或表格文件时才生成表格文件完整版。具体最终输出规则统一见“二十四、最终输出要求”。

---

## 十四、聊天文字速览版规范

聊天版用于用户即时判断，不堆完整用例表。

### 14.1 聊天版必须包含

1. 本次资料质量结论。
2. 本次强核对门禁结论。
3. 接口基本判断。
4. 核心风险。
5. 待确认问题。
6. 核心用例速览；L0 / L1 阻塞时改为五列轻量测试方向。
7. 表格文件触发状态或文件说明。
8. 测试结论。

### 14.2 聊天版推荐结构

#### 1. 资料质量与门禁结论

| 项目        | 结论                     |
| --------- | ---------------------- |
| 资料等级      | L0 / L1 / L2 / L3 / L4 |
| 是否可生成正式用例 | 是 / 部分 / 否             |
| 是否存在阻塞型问题 | 是 / 否                  |
| 本次输出策略    | 完整生成 / 部分生成 / 先澄清      |
| 主要缺口      | 简要说明                   |

#### 2. 接口基本判断

| 项目       | 内容                               |
| -------- | -------------------------------- |
| 接口名称     | 按资料填写                            |
| 请求方式     | GET / POST / PUT / DELETE        |
| 接口地址     | 按资料填写                            |
| 接口类型     | 查询 / 创建 / 修改 / 删除 / 状态 / 回调 / 资产 |
| 是否鉴权     | 是 / 否 / 待确认                      |
| 是否有数据副作用 | 是 / 否 / 待确认                      |

#### 3. 核心风险

| 风险点 | 风险说明 | 优先级 |
| --- | ---- | --- |

核心风险不超过 8 条，必须优先展示 P0 / P1 风险。

#### 4. 待确认问题

| 问题 ID | 问题分类 | 需要确认的问题 | 门禁类型 | 优先级 |
| ----- | ---- | ------- | ---- | --- |

只列真正影响测试预期或覆盖完整性的问题。

#### 5. 核心用例速览

| 用例 ID | 用例标题 | 优先级 | 确认状态 |
| ----- | ---- | --- | ---- |

仅在门禁允许生成正式用例时，聊天版展示 P0 和关键 P1 用例且不展示完整步骤。L0 / L1 存在阻塞型缺口时，本节替换为 6.5 定义的五列轻量测试方向。

#### 6. 测试结论

用 3～5 行说明：

1. 是否可以开测。
2. 哪些内容可以直接测。
3. 哪些预期必须待确认。
4. 哪些风险最高。
5. 若用户明确要求文件，说明完整用例文件状态；否则说明本次未触发表格文件生成。

---

## 十五、表格文件完整版规范

表格文件用于编辑、评审、交付、归档。
文件生成条件统一见“二十四、最终输出要求”。

### 15.1 文件命名规则

默认命名：

`接口名称_单接口测试用例_YYYYMMDD.xlsx`

如有模块名：

`模块名_接口名称_单接口测试用例_YYYYMMDD.xlsx`

如多次生成，追加版本号：

`接口名称_单接口测试用例_YYYYMMDD_v1.xlsx`

---

## 十六、表格文件 Sheet 结构

| Sheet 名称  | 是否必须 | 用途                                    |
| --------- | ---: | ------------------------------------- |
| 01_资料质量判断 |   必须 | 判断资料完整度、门禁结论、可测范围                     |
| 02_接口基础信息 |   必须 | 固化接口名称、URL、Method、Header、Body、响应      |
| 03_参数规则分析 |   必须 | 分析 Header、Query、Path、Body、Response 字段 |
| 04_待确认问题  |   必须 | 给产品 / 后端确认的问题                         |
| 05_测试点清单  |   必须 | 测试设计中间层                               |
| 06_测试用例   |   必须 | 通过门禁后的完整可执行用例；未通过时记录阻塞结论              |
| 07_风险与自检  |   必须 | 风险声明、不可测项、覆盖自检                        |
| 08_接口原始信息 |   可选 | 原始接口资料摘要或解析结果                         |

生成表格文件时，`04_待确认问题` 和 `06_测试用例` 不得省略；L0 / L1 存在阻塞型缺口时，`06_测试用例` 只记录门禁结论，不填充正式十列用例行。

---

## 十七、各 Sheet 固定字段

### 17.1 01_资料质量判断

| 字段名      |
| -------- |
| 检查项      |
| 判断结果     |
| 说明       |
| 门禁类型     |
| 是否影响正式用例 |

### 17.2 02_接口基础信息

| 字段名  |
| ---- |
| 字段   |
| 内容   |
| 来源   |
| 确认状态 |
| 备注   |

### 17.3 03_参数规则分析

| 字段名   |
| ----- |
| 参数名   |
| 参数位置  |
| 类型    |
| 是否必填  |
| 示例值   |
| 文档规则  |
| 测试关注点 |
| 缺失信息  |
| 确认状态  |
| 置信度   |

### 17.4 04_待确认问题

| 字段名     |
| ------- |
| 问题 ID   |
| 问题分类    |
| 需要确认的问题 |
| 当前依据    |
| 不确认的影响  |
| 建议回答人   |
| 门禁类型    |
| 优先级     |
| 确认结果    |

### 17.5 05_测试点清单

| 字段名    |
| ------ |
| 测试点 ID |
| 测试维度   |
| 测试点描述  |
| 依据     |
| 置信度    |
| 确认状态   |
| 优先级    |
| 备注     |

### 17.6 06_测试用例

仅在强核对门禁允许生成正式用例时使用以下十列；L0 / L1 存在阻塞型缺口时，按 6.5 的降级输出形态执行。

统一十列表头：

| 用例 ID | 所属模块 | 用例标题 | 验证功能点 | 前置条件 | 测试步骤 | 预期结果 | 优先级 | 执行结果（通过 / 不通过 / 未执行） | 备注 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |

字段顺序必须固定如下：

| 顺序 | 字段名                  |
| -: | -------------------- |
|  1 | 用例 ID                |
|  2 | 所属模块                 |
|  3 | 用例标题                 |
|  4 | 验证功能点                |
|  5 | 前置条件                 |
|  6 | 测试步骤                 |
|  7 | 预期结果                 |
|  8 | 优先级                  |
|  9 | 执行结果（通过 / 不通过 / 未执行） |
| 10 | 备注                   |

说明：

1. 默认执行结果为“未执行”。
2. 用例 ID 必须唯一、连续。
3. 验证功能点必须用编号列出该用例实际验证的功能点、规则点、异常点、边界点、权限点、数据一致性点、幂等点或并发点。
4. 测试步骤必须可执行。
5. 预期结果必须可判定。
6. 备注中必须标注“待确认”或“合理假设”。

### 17.7 07_风险与自检

| 字段名    |
| ------ |
| 检查项    |
| 结论     |
| 风险说明   |
| 是否影响开测 |
| 处理建议   |

### 17.8 08_接口原始信息

| 字段名    |
| ------ |
| 信息类型   |
| 原始内容摘要 |
| 解析结果   |
| 备注     |

---

## 十八、测试用例编写规则

### 18.1 用例标题规则

用例标题格式建议：

`【类型】验证目标`

示例：

* 【正向】合法用户获取钻石余额成功
* 【鉴权】未传 Authorization 时拒绝访问
* 【参数】缺失必填参数时返回失败
* 【资产】余额为 0 时返回 diamond=0
* 【越权】用户不能查询其他用户资产
* 【并发】同一用户并发查询余额结果稳定

### 18.2 前置条件规则

前置条件必须包含：

1. 测试环境。
2. 测试账号。
3. 登录态 / token。
4. 角色权限。
5. 数据准备。
6. 第三方依赖。
7. 特殊状态。
8. 无法准备的数据需备注说明。

### 18.3 测试步骤规则

测试步骤必须包含：

1. 请求方式。
2. 接口地址。
3. Header。
4. Query / Path / Body / FormData。
5. 关键测试数据。
6. 调用方式。
7. 必要的查询或校验动作。

### 18.4 预期结果规则

预期结果必须包含：

1. HTTP 状态。
2. 业务 code。
3. msg。
4. response 字段。
5. 字段类型。
6. 字段取值。
7. 数据副作用。
8. 错误响应。
9. 安全要求。
10. 若具体值缺失，必须标注待确认。

禁止出现以下模糊表达：

* 返回正常
* 系统正常
* 处理成功
* 符合预期
* 无异常
* 正确返回

必须改为：

* HTTP 状态码为 200
* 业务 code 为接口约定成功码
* 返回字段包含 `data.fieldName`
* 字段类型为 number/string/boolean
* 不返回其他用户数据
* 不产生重复扣减 / 重复发放
* 具体 code/msg 待确认

---

## 十九、错误码缺失时的固定表达

当错误码文档缺失时，异常用例预期统一使用以下表达：

> 接口应返回失败响应，不应返回成功业务结果；不应产生数据副作用；具体 HTTP 状态码、业务 code、msg 待产品 / 后端确认。

当鉴权错误码缺失时：

> 接口不应返回用户数据或敏感信息；应返回鉴权失败响应；具体 HTTP 状态码、业务 code、msg 待产品 / 后端确认。

当参数错误码缺失时：

> 接口应识别参数错误并返回失败响应；不应出现 500 未处理异常；具体 code/msg 待产品 / 后端确认。

当幂等规则缺失时：

> 重复请求不应造成重复扣减、重复发放、重复创建或重复状态变更；具体幂等返回规则待产品 / 后端确认。

---

## 二十、不同资料等级下的输出策略

| 资料等级 | 聊天版输出                         | 表格文件输出（仅用户明确要求）                         |
| ---- | ----------------------------- | ---------------------------------------- |
| L0   | 微需求澄清、范围边界、五列轻量测试方向          | 澄清问题表、轻量测试点表；`06_测试用例` 仅记录阻塞结论       |
| L1   | 资料缺口、阻塞问题、候选风险、五列轻量测试方向      | 资料质量判断、待确认问题、轻量测试点；`06_测试用例` 仅记录阻塞结论 |
| L2   | 契约分析、合理假设、部分核心用例              | 参数解析、待确认问题、部分可执行用例                     |
| L3   | 风险摘要、核心用例速览                   | 完整单接口测试用例文件                            |
| L4   | 完整结论、核心风险、用例摘要                | 完整用例、覆盖矩阵、风险自检                         |

---

## 二十一、资料不足时的处理边界

### 21.1 可以做的事

1. 输出资料质量判断。
2. 输出缺失信息。
3. 输出待确认问题。
4. 输出接口风险。
5. 输出轻量测试点。
6. 仅在对应场景不受阻塞型缺口影响时，输出可确认部分的用例。
7. L0 / L1 的候选内容按 6.5 输出为五列轻量测试方向。
8. 标注合理假设。
9. 标注置信度。

### 21.2 不可以做的事

1. 不得生成完整正式用例并声称可直接执行。
2. 不得写死错误码。
3. 不得写死业务提示。
4. 不得写死枚举范围。
5. 不得写死权限边界。
6. 不得写死金额口径。
7. 不得写死状态流转。
8. 不得写死第三方回调规则。

---

## 二十二、风险声明模板

当资料不完整时，必须输出风险声明。

推荐表达：

> 本次用例基于用户提供的接口资料生成。文档中未明确的错误码、鉴权规则、字段枚举、业务规则、数据副作用、幂等策略或并发策略，已在待确认问题和备注中标记。未确认前，相关用例不应作为最终验收口径，只能作为测试设计候选或风险提醒。

---

## 二十三、最终自检清单

输出前必须自检。

| 检查项                 | 是否必须 |
| ------------------- | ---: |
| 是否判断输入资料等级          |   必须 |
| 是否输出强核对门禁结论         |   必须 |
| 是否区分阻塞型、非阻塞型、扩展型待确认 |   必须 |
| 是否通过“二十五、禁止事项总表”复核 |   必须 |
| 是否标注合理假设            |   必须 |
| 是否标注置信度             |   必须 |
| 是否覆盖正常流程            |   必须 |
| 是否覆盖接口契约            |   必须 |
| 是否覆盖必填、类型、边界、枚举     |   必须 |
| 是否覆盖鉴权和权限           |   必须 |
| 是否分析数据副作用           |   必须 |
| 是否识别幂等风险            |   必须 |
| 是否识别并发风险            |   必须 |
| 是否控制用例冗余            |   必须 |
| 是否生成聊天速览版           |   必须 |
| 用户明确要求文件时，是否生成表格文件完整版 |   条件必须 |
| L0 / L1 阻塞时是否仅输出五列轻量测试方向 |   必须 |
| L0 / L1 阻塞时是否避免十列正式用例行 |   必须 |
| 是否保证用例可执行、可判定       |   必须 |
| 是否保留“验证功能点”列        |   必须 |

---

## 二十四、最终输出要求

执行本 Skill 后，始终按“十四、聊天文字速览版规范”输出精炼聊天结论；用户明确要求文件、Excel、xlsx 或表格文件时，再按“十五至十七”生成可编辑、可评审、可归档的表格文件。资料门禁未通过时，两种载体都必须遵守 6.5，不得用文件请求绕过正式用例门禁。

---

## 二十五、禁止事项总表

| 禁止项           | 说明                 |
| ------------- | ------------------ |
| 禁止无资料生成完整正式用例 | 防止幻觉               |
| 禁止编造 code/msg | 错误码必须来自文档或待确认      |
| 禁止编造枚举值       | 枚举未说明必须待确认         |
| 禁止编造权限边界      | 角色、租户、用户隔离未说明必须待确认 |
| 禁止编造金额口径      | 余额、冻结、可用、赠送等口径必须确认 |
| 禁止编造状态机       | 状态流转必须来自文档         |
| 禁止把合理假设写成事实   | 必须备注说明             |
| 禁止过度保守不扩展风险   | 高风险接口必须主动提示风险      |
| 禁止机械堆字段异常     | 普通字段可合并            |
| 禁止遗漏高风险专项     | 鉴权、越权、资产、幂等、并发不能漏  |
| 禁止无文件请求时默认生成文件 | 聊天版必需，表格文件由用户明确触发  |
| 禁止只输出文件版      | 每次执行都必须有聊天结论       |
| 禁止改变测试用例固定字段  | 保持项目规范一致           |

---

## 二十六、v0.3 版本验收标准

本 Skill v0.3 必须达到以下效果：

1. 用户提供完整单接口文档时，可以生成完整、可执行、可评审的测试用例表。
2. 用户提供半成品接口资料时，可以先识别资料缺口，不脑补业务规则。
3. 用户提供一句话需求时，不直接生成正式用例，而是先输出澄清问题和轻量测试方向。
4. 对接口文档缺少错误码、枚举、鉴权、业务规则的情况，能清晰标记待确认。
5. 对高风险接口，能主动扩展鉴权、越权、资产、幂等、并发、数据一致性风险。
6. 所有扩展内容必须标注依据、置信度和确认状态。
7. 聊天版必须简洁、清晰、一目了然。
8. 用户明确要求文件时，表格文件必须完整、可编辑、可交付。
9. 用例必须保留固定字段和“验证功能点”列。
10. 输出前必须完成自检，避免幻觉、冗余和不可执行用例。

---

## 二十七、v0.3 核心总结

本 Skill 的核心不是“生成更多用例”，而是：

> 在不失真的前提下，充分发挥测试工程师的风险扩展能力；
> 在强核对门禁下，区分事实、假设、待确认和不可测；
> 最终交付一份便于快速阅读的聊天结论；用户明确要求文件时，再交付可编辑、可评审、可归档的表格文件。

本 Skill 的工作方式可以概括为：

**分析内核 + 聊天必需、文件按需 + 强核对门禁 + 受控扩展机制。**

## 二十八、互斥路由与组合调用

普通单接口测试默认由本 Skill 作为主 Skill；用户显式要求“精炼版、快速版、短版、低上下文”时，应改由单接口精炼版作为主 Skill。多个接口或完整调用链改走多接口链路；以 PRD 为主要输入改走需求工作台；正式服目标优先由正式服验证作为主 Skill。

若任务还需要需求澄清、已有用例审计或正式服安全门禁，只能建议最多一个辅助 Skill。调用辅助 Skill 前必须先输出并暂停等待确认：

```text
本次 Skill 调用说明
主 Skill：single-api-test-full
辅助 Skill：<名称或无>
职责分工：<主 Skill 负责什么；辅助 Skill 补充什么>
最终只生成一套结果。是否确认同时调用？
```

用户确认前，不加载或执行辅助 Skill，也不生成最终文件；不得把沉默视为同意。用户拒绝时，本 Skill 继续执行，并明确说明未做辅助分析可能造成的覆盖限制。聊天开头、Excel 总览和 HTML 总览均记录主 Skill、辅助 Skill、职责分工与确认结果。

## 二十九、Excel 与 HTML 双交付合同

用户只要求聊天内容时不创建文件。用户明确要求文件、Excel、xlsx、HTML、归档表或可下载文件时，默认基于**同一份报告 JSON**同时交付 `.xlsx` 和 `.html`，不得只交付其中一种，也不得分别生成两套用例数据。

正式用例继续使用原十列顺序。执行结果固定为 `未执行 / 通过 / 不通过 / 待定`：`待定`表示已经执行，但需求、产品、研发或测试口径存在歧义，暂时不能定性为缺陷；备注是否填写由测试人员自行决定。模块分割行不设置下拉。Excel 使用四状态数据验证；`不通过`整行淡红，`待定`整行淡灰，`通过`和`未执行`保留原模块色与优先级色。

HTML 必须是无外部请求的单文件离线执行版，含搜索、模块/优先级/状态筛选、冻结表头、四状态统计、状态下拉和按报告隔离的 `localStorage` 自动保存；本地存储不可用时显示警告，但页面仍可执行。

生成时在安装包目录执行：

```bash
node scripts/render-test-assets.mjs --input report.json --output-dir output --basename 单接口测试用例
```

必须检查两个文件都存在且可打开后才能宣称完成，并返回两个文件链接。

