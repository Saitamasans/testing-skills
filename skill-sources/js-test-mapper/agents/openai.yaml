interface:
  display_name: "Web JS 测试系统建图"
  short_description: "只读建图，主交付是可点击的流程 HTML"
  default_prompt: "请在我已授权的测试环境中，对这个 Web URL 做只读测试系统建图。主交付必须是可点击的 HTML 流程图（哪块连哪块、线上写关系、点节点看出已确认/未验证/建议测什么），不要用 Word/Markdown/表格代替图。不执行改变业务状态的操作，不主动调用或重放业务 API，不保存密码、Cookie 或 Token。"
