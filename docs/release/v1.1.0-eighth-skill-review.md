# 第八个 Skill 更新后审计

审计对象：`skills/web-api-test-execution-evidence`

审计方法：使用 `skill-review` 的结构、描述、工作流、Token 效率和反模式五个维度，补充官方 `quick_validate.py`、工程回归、真实执行、四状态一致性和媒体完整性校验。

## 结论

- P0/P1 问题：0。
- 发布包 `SKILL.md`：67 行、344 词，低于 500 行上限；完整源文档为 227 行、622 词，由构建流程拆分为主文件与按需引用。
- 目录：1 个主文件、5 个按需引用、2 个可执行脚本、1 个 Runner 固定资产、1 个发现元数据文件。
- 官方结构校验：`Skill is valid!`。
- Runner 回归：162/162 通过；仓库渲染、安装和发布包回归：24/24 通过；TypeScript 类型检查和构建通过。
- 真实执行：18 条测试用例（Test Cases）、167 个动作；四状态为通过 9、不通过 2、待定 1、未执行 6。
- Excel、HTML、`projected-report.json` 和 `run-result.json` 的四状态逐项一致；Excel 保留 5 个工作表和统一十列顺序。
- 原始版和教程版视频均为 H.264、1920×1080、60 FPS、127.034 秒，并通过全片解码；21 个教程验收链接全部存在。

## 优点

1. 顶部硬规则直接约束最大风险：只执行已有测试用例（Test Cases），不生成用例、不猜环境、不静默转换字段、不泄露密钥。
2. `description` 同时覆盖自动执行、固定 manifest、证据生成、Excel/HTML 回填和产物验证等自然触发意图，并明确排除用例生成、需求澄清和用例审计，互斥路由清楚。
3. 输入、风险、定位器与断言、Runner 命令、CI 报告拆成五份按需引用，发布包主文件保持在 67 行，没有把所有分支一次性塞进上下文。
4. 执行前强制展示主/辅 Skill、manifest hash、目标 origin、风险、动作数量和环境变量名；用户确认前禁止 Web/API/数据库动作。
5. 交互执行完整展示执行准备、用例预告、实时执行、证据收集和结果中心；正式 Web PNG 隐藏驾驶舱，桌面录屏保留完整视觉过程。
6. 最终自检和反模式明确保留“未执行、通过、不通过、待定”四状态，避免把待定误算成研发 Bug。

## 建议

- 当前没有发布阻断项。若后续 `SKILL.md` 超过 200 行，优先把五阶段视觉说明的实现细节继续下沉到 `references/runner-commands.md`，主文件只保留行为契约，避免随功能增长回到单体长文档。
- 如果后续真实使用中出现模型跳过准备度或审批步骤，可在“工作入口”后增加不超过 6 项的执行进度清单，只引用现有章节，不重复规则正文。

## 发布验收

Runner `1.0.4` 已发布到 GitHub Release：`testing-runner-v1.0.4`。公开资产 `saitamasans-testing-runner-1.0.4.tgz` 大小为 `22,019,328` 字节，SHA-256 为 `5f1e1f03c8131327e3518fd5b687ff4a1c0214dd99a36bd00b883524c43c49fb`，与 Skill 内固定清单一致。

本轮真实执行生成 7 张正式 Web PNG、51 份 API 请求/响应、86 份 API 断言、Excel、HTML、JSON、执行日志和 Playwright Trace。第八个 Skill 专用教程保留原始未剪辑录屏，并生成带可选中文字幕的教程版、八阶段独立 PNG、时间线、媒体校验和 3,484 文件的 SHA-256 证据索引。

2026-07-17 已从 GitHub `main` 安装到全新的 D 盘隔离目录。安装器识别到仓库内 8 个 Skill，只选择并安装 `web-api-test-execution-evidence`；Codex 等支持全局 Skill 的目标安装成功，安装器对不支持全局安装的 PromptScript 给出独立提示，不影响已完成的 Codex 安装。随后从该安装目录连续启动 Runner：首次自动下载并校验 `1.0.4`，10.395 秒、退出码 0；第二次复用同一缓存，1.975 秒、退出码 0；再次查询版本返回 `1.0.4`。缓存包含 3,224 个文件、142,163,975 字节，整个 Runner 准备过程不需要 npm 账号或手工安装命令。

至此 GitHub 安装、Runner 首次准备和缓存复用门禁均完成。本轮未修改七个既有 Skill 的名称、数量或表达习惯。
