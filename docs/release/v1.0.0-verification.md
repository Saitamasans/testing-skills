# v1.0.0 本地验证记录

## 范围

- 七个中文源文件名称与位置保持不变；
- 七个标准 Skill 安装包；
- 五个生成类 Skill 的 Excel/HTML 共用渲染器；
- README、MIT License、GitHub Actions 和安装脚本；
- 本地结果已于 2026-07-14 由用户人工验收通过；本文随后作为 v1.0.0 发布证据提交。

## 生成包行数

| Package | SKILL.md 行数 |
|---|---:|
| single-api-test-full | 435 |
| single-api-test-concise | 350 |
| multi-api-flow-test | 353 |
| requirement-test-workbench | 469 |
| production-verification-test | 445 |
| test-case-quality-audit | 451 |
| requirement-clarification-test | 361 |

完整版的低频细则按需拆入 `references/`，内容保留，不以删门禁或改写用户表达换取行数。

## 双格式样例

- `build/test-output/sample.xlsx`
- `build/test-output/sample.html`
- `build/test-output/sample-previews/`：三个 Sheet 的渲染预览
- `build/portable-test2/portable.xlsx`：无第三方依赖 OOXML 兜底样例

已验证：统一十列、四状态、模块分割行无下拉、正式行有下拉、不通过淡红、待定淡灰、通过/未执行保留基础样式、冻结表头、筛选、按报告隔离的 localStorage 逻辑、零外部资源引用。

## 环境说明

1. 当前发布环境无系统级 `npx`；七条安装路径已由仓库测试逐项验证，推送后再使用 Codex Skill Installer 从 GitHub 做等价下载验证。
2. HTML 自动化运行库缺少 `playwright-core`，对应自动化用例明确跳过；用户已在应用内打开实际 HTML 报告并完成人工验收。
3. 七个包均通过 Codex 官方 `quick_validate.py` 校验，完整版按需 reference、五个渲染器包和两个非渲染器包的边界均通过合同测试。

以上环境差异已如实记录，不影响已完成的人工验收与本地结构/输出合同验证。
